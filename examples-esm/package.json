{
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm run --prefix ../ build && npm run clean",
    "build": "npx tsc --build ./tsconfig.json",
    "clean": "npx shx rm -rf build"
  },
  "dependencies": {
    "@apollo/server": "^4.6.0",
    "reflect-metadata": "^0.1.13",
    "type-graphql": "file:.."
  }
}
